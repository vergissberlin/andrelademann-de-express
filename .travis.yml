language: node_js

node_js:
  - '5'
  - '6'
  - 'stable'

before_script:
  - gem install compass scss_lint
  - rvm install 2.2.2
  - npm install coveralls grunt-cli eslint marked node-gyp -g

cache:
  bundler: true
  directories:
  - node_modules
