{{> header}}

{{#if profile}}
	{{> wysiwyg-article}}
	{{> wysiwyg}}
{{else}}
    <article>
        <section>
            <header>
                <h1>{{{article.title}}}</h1>
                <p class="lead">{{{article.teaser}}}</p>
            </header>
            <p>{{{article.text}}}</p>
            <p class="text-muted">{{createdAt}}</p>
            <p class="text-muted">Latest update {{updatedAt}}</p>

        </section>
    </article>

		{{>prism}}

		{{>author}}

		{{>disqus}}
{{/if}}
