{{#if profile}}
	{{> wysiwyg-article}}
	{{> wysiwyg}}
{{else}}
	<article itemscope itemType="http://schema.org/BlogPosting">
		<div class="hero is-medium is-primary" id="header">
			<header class="hero-body">
				<div class="container">
					<h1 class="title">
						{{{article.title}}}
					</h1>
					{{#if article.subtitle}}
						<h2 class="subtitle">
							{{{article.subtitle}}}
						</h2>
					{{/if}}
				</div>
			</header>
		</div>
		<div class="section">
			<div class="container">
				<div class="content">
					<time class="is-size-7" datetime="{{datetime article.createdAt}}"
								itemprop="datePublished"
								content="{{article.createdAt}}">
						{{{__ 'Created at'}}}&nbsp;{{formatDate article.createdAt}}
						{{#if article.updatedAt}}, {{__ 'updated at'}}
							&nbsp;{{formatDate article.updatedAt}}{{/if}}
					</time>
					<p class="subtitle" itemprop="description">
						{{{article.teaser}}}
					</p>
				</div>
				<div class="content" itemprop="articleBody">{{{article.text}}}</div>
			</div>
		</div>
	</article>

	{{>prism}}

	<aside>
		<div class="container">
			<div class="content">
				{{>author}}
			</div>
		</div>
	</aside>

	{{> image-background image=article.image}}

{{/if}}
