{{> header}}

{{#if profile}}
	{{> wysiwyg-article}}
	{{> wysiwyg}}
{{else}}
    <article>
        <section>
            <header>
                <h1>{{{article.title}}}</h1>
                <p class="text-muted">Created at {{formatDate article.createdAt}}</p>
                <p class="lead">{{{article.teaser}}}</p>
            </header>
            <p>{{{article.text}}}</p>
        </section>
    </article>

		{{>prism}}

		{{>author}}

		{{>disqus}}
{{/if}}
